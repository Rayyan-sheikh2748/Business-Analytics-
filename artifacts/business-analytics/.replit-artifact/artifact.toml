kind = "web"
previewPath = "/"
title = "Business Analytics Dashboard"
version = "1.0.0"
id = "artifacts/business-analytics"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25192

[services.development]
run = "pnpm --filter @workspace/business-analytics run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/business-analytics", "run", "build" ]
publicDir = "artifacts/business-analytics/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25192"
BASE_PATH = "/"
